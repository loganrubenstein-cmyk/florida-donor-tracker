name: Weekly — Fund Transfers

on:
  schedule:
    - cron: '0 8 * * 0'
  workflow_dispatch:

concurrency:
  group: weekly-transfers
  cancel-in-progress: false

env:
  SUPABASE_DB_URL:               ${{ secrets.SUPABASE_DB_URL }}
  SUPABASE_SERVICE_ROLE_KEY:     ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

jobs:
  transfers:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      issues: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Download committee registry
        uses: ./.github/actions/run-script
        with:
          script: 02_download_registry.py
      - name: Import committee registry
        uses: ./.github/actions/run-script
        with:
          script: 05_import_registry.py
      - name: Scrape fund transfers
        uses: ./.github/actions/run-script
        with:
          script: 11_scrape_transfers.py
      - name: Open issue on failure
        if: failure()
        uses: ./.github/actions/report-failure
        with:
          title: "[pipeline-failure:low] weekly-transfers"
          labels: pipeline-failure,severity:low,data-update
          primary-log: data/logs/11_scrape_transfers.py.log
